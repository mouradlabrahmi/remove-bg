# from graphene import relay
# from . import queries, mutations
# from graphene_django.filter import DjangoFilterConnectionField

# class ImageQueries:
#     image = relay.Node.Field(queries.ImageNode)
#     images = DjangoFilterConnectionField(queries.ImageNode)


# class ImageMutations:
#     create_image = mutations.CreateImage.Field()
